services:
    db:
        container_name: db
        image: postgres:16-alpine
        ports:
            - "5432:5432"
        restart: always
        shm_size: 128mb
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres

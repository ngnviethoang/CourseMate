export function CourseMateLogoIcon({ className }: { className?: string }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img src="/favicon.ico" alt="CourseMate" className={className} />
  )
}
